# install-developer-script-tools
install brew and asdf without sudo

# how to
clone repository

use comand 
go to clone folder
```bash
cd install-developer-script-tools
```

check if install.sh is present
```bash
ls
```

make install.sh execute-able

```bash
chown +x install.sh
```

run script
```bash
./install.sh
```
